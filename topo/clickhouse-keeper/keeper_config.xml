<?xml version="1.0"?>
<!--
  Standalone ClickHouse Keeper configuration (Raft quorum member id=3).
  This is a lightweight keeper-only container — it does NOT run a CH server.
  Purpose: provide the 3rd Raft vote so the cluster can survive
           1 keeper node failure (majority = 2 of 3).

  Data stored in: /var/lib/clickhouse-keeper  (mapped via docker volume)
-->
<clickhouse>
    <keeper_server>
        <tcp_port>9181</tcp_port>
        <server_id>3</server_id>
        <log_storage_path>/var/lib/clickhouse-keeper/coordination/log</log_storage_path>
        <snapshot_storage_path>/var/lib/clickhouse-keeper/coordination/snapshots</snapshot_storage_path>

        <coordination_settings>
            <operation_timeout_ms>20000</operation_timeout_ms>
            <session_timeout_ms>30000</session_timeout_ms>
            <raft_logs_level>warning</raft_logs_level>
        </coordination_settings>

        <raft_configuration>
            <server>
                <id>1</id>
                <hostname>clickhouse-blue-1</hostname>
                <port>9234</port>
            </server>
            <server>
                <id>2</id>
                <hostname>clickhouse-blue-2</hostname>
                <port>9234</port>
            </server>
            <server>
                <id>3</id>
                <hostname>clickhouse-keeper</hostname>
                <port>9234</port>
            </server>
        </raft_configuration>
    </keeper_server>

    <logger>
        <level>warning</level>
        <console>1</console>
    </logger>
</clickhouse>
